{
  "name": "foo",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && esbuild src/index.ts --target=es2022 --sourcemap --bundle --platform=node --outfile=dist/index.js",
    "zip": "cd dist && bestzip ../output.zip *.js && cd ..",
    "build-output-zip": "npm run build && npm run zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/node18": "^2.0.0",
    "@types/node": "^18.16.1",
    "bestzip": "^2.2.1",
    "esbuild": "^0.17.18",
    "typescript": "^5.0.4"
  }
}
